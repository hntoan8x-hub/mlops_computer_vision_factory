# ----------------------------------------------------
# 1. CORE ML FRAMEWORK (Computer Vision)
# ----------------------------------------------------
# Cần cho Trainer/Predictor/Feature Extraction
torch>=1.13.0
torchvision>=0.14.0
torchaudio>=0.13.0
# Tăng tốc tính toán (Nếu sử dụng Cuda/GPU)
# CUDA-related dependencies should be managed by the base image if possible, 
# but if explicit pinning is needed, they go here (e.g., torchaudio+cu117)

# ----------------------------------------------------
# 2. MLOPS & ARTIFACT MANAGEMENT
# ----------------------------------------------------
# Cần cho BaseTracker, BaseRegistry, Model Loading
mlflow>=2.1.1
boto3>=1.26.0  # Cần cho việc lưu trữ artifact (S3, MinIO)
pydantic>=1.10.4 # Cần cho tất cả các Config Schema validation
requests>=2.28.1 # Cần cho AlertReporter và các tiện ích HTTP khác
typing-extensions>=4.4.0 # Hỗ trợ Type Hint nâng cao

# ----------------------------------------------------
# 3. FASTAPI/INFERENCE SERVING
# ----------------------------------------------------
# Cần cho Inference Role Image
fastapi>=0.95.0
uvicorn[standard]>=0.20.0 # Standard includes tools like httptools and websockets
gunicorn>=20.1.0 # Cần cho Inference Role Production CMD

# ----------------------------------------------------
# 4. DATA HANDLING & UTILITIES
# ----------------------------------------------------
# Cần cho DataLoader/Preprocessing/Monitoring
numpy>=1.24.2
pandas>=1.5.3
scipy>=1.10.0 # Cần cho các thống kê như KS-Test, KL/JS Divergence
Pillow>=9.4.0 # Thư viện xử lý ảnh (thường dùng trong CVDataset/Preprocessing)
nltk>=3.8.1 # Cần cho trainer.Dockerfile (ví dụ: mô hình lai/metadata)
scikit-learn>=1.2.1 # Cần cho Classification Metrics (Evaluation)
python-multipart>=0.0.5 # Cho FastAPI Form/File Uploads

# ----------------------------------------------------
# 5. MONITORING & RETRAINING TRIGGER (Reporters & Utilities)
# ----------------------------------------------------
prometheus-client>=0.16.0 # Cần cho PrometheusReporter
# Thư viện cho Alerting (Giả định)
# slack-sdk>=3.20.1 # Nếu dùng Slack SDK thay vì webhook đơn giản
# python-json-logger>=2.0.7 # Cho LogReporter
# python-grafana-api>=1.0.0 # Nếu sử dụng GrafanaDashboardReporter

# ----------------------------------------------------
# 6. FEATURE STORE & VECTOR SEARCH (Giả định)
# ----------------------------------------------------
# Cần cho FeatureStoreOrchestrator/VectorStoreFactory
# Giả định sử dụng FAISS cho Vector Search Engine
faiss-cpu>=1.7.3